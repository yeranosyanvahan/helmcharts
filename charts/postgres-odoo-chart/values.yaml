externalDatabase:
  username: user
  password: password
  cluster:
    name: postgres-public000-asset
    namespace: asset-public-postgres
ingress:
  enabled: true
  hostname: od308.web.miom.am
  wwwPrefix: false
  annotations:
    kubernetes.io/ingress.class: traefik
persistence:
  accessModes:
    - ReadWriteOnce
  size: 12Gi
  storageClass: local-path
odoo:
  init:
    adminPassword: admin
  odooConfOptions: |
    workers = 0
    limit_memory_hard = 512000000
  replicas: 1